# Руководство по миграции на v2.0

## 🚀 Переход с v1.0 на v2.0

### Что изменилось

#### ✅ Решены проблемы с токенами:
- **Автоматическое предотвращение обрывов** - система предсказывает и предотвращает обрезание ответов
- **Умное сжатие контекста** - сохранение важной информации при сжатии диалогов
- **Адаптивные лимиты токенов** - индивидуальные настройки для каждого пользователя
- **Автоматическое продолжение** - если ответ обрезался, система его продолжает

#### ✅ Новая архитектура:
- **Модульная структура** - код разделен на логические компоненты
- **Интеграция компонентов** - все части работают как единая система
- **Мониторинг в реальном времени** - отслеживание и автоматическая оптимизация
- **Кэширование** - снижение затрат и повышение скорости

### 📋 Пошаговая миграция

#### Шаг 1: Подготовка
```bash
# Создайте резервную копию текущего бота
cp hr_psychoanalyst_bot.py hr_psychoanalyst_bot_v1_backup.py

# Установите новые зависимости
pip install -r requirements_v2.txt
```

#### Шаг 2: Настройка переменных окружения
```bash
# Добавьте новые переменные (если нужно)
export BOT_TOKEN="your_telegram_bot_token"
export OPENAI_API_KEY="your_openai_api_key"
export PAYMENT_TOKEN="your_payment_token"  # опционально
```

#### Шаг 3: Тестирование новой архитектуры
```bash
# Запустите тесты
python test_architecture.py
```

#### Шаг 4: Запуск нового бота
```bash
# Запустите новую версию
python hr_psychoanalyst_bot_v2.py
```

### 🔄 Совместимость

#### ✅ Что работает без изменений:
- Все команды бота (`/start`, `/help`, `/cancel`)
- Тест самооценки (`/self_esteem`)
- Экспресс-анализ
- Полный анализ
- Психологическая консультация

#### ⚠️ Что изменилось:
- **Внутренняя архитектура** - код переписан, но функциональность сохранена
- **Управление токенами** - теперь автоматическое
- **Кэширование** - добавлено для повышения производительности
- **Мониторинг** - добавлены новые команды для администраторов

### 🆕 Новые возможности

#### Для пользователей:
- **Более быстрые ответы** - благодаря кэшированию
- **Более качественные ответы** - благодаря адаптивным промптам
- **Отсутствие обрывов** - автоматическое управление токенами

#### Для администраторов:
- `/stats` - статистика системы
- `/optimize` - оптимизация для пользователя
- Автоматический мониторинг и оптимизация

### 🐛 Решение проблем

#### Проблема: "ModuleNotFoundError"
**Решение:**
```bash
# Убедитесь, что установлены все зависимости
pip install -r requirements_v2.txt

# Проверьте, что src в PYTHONPATH
export PYTHONPATH="${PYTHONPATH}:$(pwd)/src"
```

#### Проблема: "Config validation error"
**Решение:**
```bash
# Проверьте переменные окружения
echo $BOT_TOKEN
echo $OPENAI_API_KEY

# Или создайте .env файл
echo "BOT_TOKEN=your_token" > .env
echo "OPENAI_API_KEY=your_key" >> .env
```

#### Проблема: "Database error"
**Решение:**
```bash
# Удалите старую базу данных (если нужно)
rm psychoanalyst.db

# Новая версия создаст базу автоматически
```

### 📊 Сравнение версий

| Функция | v1.0 | v2.0 |
|---------|------|------|
| Управление токенами | Ручное | Автоматическое |
| Архитектура | Монолитная | Модульная |
| Кэширование | Нет | Есть |
| Мониторинг | Базовый | Продвинутый |
| Оптимизация | Ручная | Автоматическая |
| Обрывы ответов | Возможны | Предотвращены |
| Скорость | Стандартная | Улучшенная |
| Затраты на API | Высокие | Оптимизированные |

### 🎯 Результат миграции

После миграции вы получите:

1. **Решенную проблему с токенами** - ответы больше не обрезаются
2. **Снижение затрат** - благодаря кэшированию и оптимизации
3. **Повышение качества** - адаптивные промпты и мониторинг
4. **Автоматическую оптимизацию** - система сама улучшает свою работу
5. **Масштабируемость** - легко добавлять новые функции

### 🔧 Настройка после миграции

#### Оптимальные настройки:
```python
# В src/core/config.py
MAX_TOKENS = 4000          # Для большинства случаев
CACHE_TTL = 3600           # 1 час для кэша
MONITORING_ENABLED = True   # Включить мониторинг
AUTO_OPTIMIZATION = True    # Автоматическая оптимизация
```

#### Мониторинг работы:
```bash
# Проверка статистики
# В Telegram: /stats

# Проверка логов
tail -f bot.log
```

### 🆘 Поддержка

Если возникли проблемы:

1. **Проверьте логи** - в файле `bot.log`
2. **Запустите тесты** - `python test_architecture.py`
3. **Проверьте конфигурацию** - переменные окружения
4. **Откатитесь на v1.0** - если критично

### 🎉 Заключение

Миграция на v2.0 решает все проблемы с токенами и значительно улучшает работу бота. Система теперь работает полностью автоматически, обеспечивая максимальное качество при минимальных затратах.

**Время миграции:** ~30 минут  
**Простой сервиса:** ~5 минут  
**Результат:** Полностью автоматизированная система без проблем с токенами